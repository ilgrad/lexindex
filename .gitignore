/target
/dist
/.venv
/.venv-docs
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.ruff_cache/
# mkdocs build output
/site/
# local planning notes / scratch measurements (not shipped)
/plans/
/local/
# agent rules, memory pointers and session state -- local only, never published
/CLAUDE.md
/.claude/
/AGENTS.md
/.cursorrules
# generated benchmark figures (regenerate with bench/*.py)
/bench/plots/
# dev-only lockfile from ad-hoc uv envs
/uv.lock

# The MPHF comparison harness is its own crate (bench/mphf_vs), outside the workspace.
bench/mphf_vs/target/
# So is the frontier harness's lexindex side (bench/frontier/frontier_lex).
bench/frontier/frontier_lex/target/
# And so is the polars plugin, which builds a wheel of its own.
polars/target/
polars/dist/
polars/.venv/
polars/uv.lock
